#!/usr/bin/env bash

usage () {
cat <<HELP_USAGE
$0

Meant to be the default cmd run by the Dockerfile upon \`docker run\`
HELP_USAGE
}

cd "$(dirname "$0")" || return

exec ../bin/tunneldigger
# exec bash
